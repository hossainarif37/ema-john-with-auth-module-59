/*
* create a new project in console.google.com
* npm install firebase
* create firebase.init.js and import getAuth to export auth
* Firebase settings > authentication > enable email and password auth


*/

// ? hosting steps
/* 
* npm install -g firebase-tools
* firebase login
* firebase init
* firebase deploy
*/
